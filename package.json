{
  "name": "athena",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "dist-electron/electron/main.js",
  "scripts": {
    "dev": "NODE_ENV=development cd renderer && npm run dev -- --strictPort & ELECTRON_DISABLE_SANDBOX=1 npx electron .",
    "build": "npm run build:ui && npm run build:electron",
    "build:ui": "cd renderer && npm run build",
    "build:electron": "npx tsc -p tsconfig.node.json",
    "start": "NODE_ENV=production npm run build && ELECTRON_DISABLE_SANDBOX=1 npx electron .",
    "start:prod": "NODE_ENV=production ELECTRON_DISABLE_SANDBOX=1 npx electron ."
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "electron": "^39.2.7",
    "pdf-parse": "^1.1.4",
    "vaul": "^1.1.2",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/electron": "^1.4.38",
    "@types/node": "^25.0.3",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
